node_modules/
.dev.vars
.wrangler/
dist/
*.log
.DS_Store
__pycache__/
*.pyc

# Données d'invités réelles : listes importées, SQL généré, liens à envoyer.
# Jamais dans le dépôt (CLAUDE.md §5) — ignoré ici pour que ce soit mécanique
# et pas seulement une question de discipline. Un CSV volontairement fictif
# peut toujours être ajouté avec `git add -f`.
prive/
*.csv
